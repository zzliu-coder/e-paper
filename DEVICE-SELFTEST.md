# 设备端自测（dev.13）

开机进入中文测试菜单。选项目、点开始，按屏幕提示操作；声音、画面、震动等需要人感知的项目，结束后在设备上点“效果正常”或“效果异常”。不用回电脑粘贴日志。没听到或没看到时不要点正常，可重测。

本机提示音/录音前恢复官方默认音频模式，并等待外置音频时钟恢复，避免蓝牙扫描模式污染下一项测试。历史 dev.11/dev.12 首次输出失败日志保留，不覆盖为通过。

菜单包含：屏幕、扬声器、震动、输入、录音回放、Wi-Fi、外置蓝牙音频、SD、电池、加速度、RTC、恢复预检、稳定性。另有验收记录页。

- 声音：音量80，三声提示音；录音回放有倒计时，录3秒后回放。
- 输入：按提示在20秒内操作触摸、底部区域和实体按键，保存原始事件。
- Wi-Fi：扫描、选择网络、在设备上输入密码；检查获取IP和重连。不代表互联网可达。密码只用于本次内存配置，不写日志。
- 蓝牙：让目标耳机/音箱进入配对模式，扫描后选择其地址，连接并确认声音。此处测试外置蓝牙音频，不代表ESP32 BLE也验收。
- SD：只在测试目录写测试文件；结果和录音也保存在SD上。
- 电池：测试期间拔插充电线；满电时充电标志可能不变化，不能据此判定硬件损坏。
- 加速度：按提示轻晃。RTC可读计时，也可手动输入时间。
- 稳定性：默认10分钟，可选8小时，可停止。没有跑完不能算8小时通过。
- 恢复：仅做本机安全设置/运行分区预检和恢复提示。真正写回原系统仍需Mac协助；预检通过不是恢复演练通过。

## 日志与结果

SD目录：`/sdcard/metalio/e-ink/sdk-selftest/`。每次启动单独JSONL日志，包含固件版本、设备身份、启动ID、测试编号和结果。每个启动日志上限2MiB；录音为WAV。`latest.json`保存同版本的最近结果；测试中断不能继承以前的通过结论。

Mac的USB服务运行且连接设备时，每5秒分块同步日志到本工程 `sessions/device-selftest/`。没连电脑也能测试并保留SD日志，下次连接再同步。服务目前不是开机自动启动项。

电脑端入口：

```sh
python3 host/service.py call selftest.open
python3 host/service.py call selftest.status
python3 host/service.py call selftest.run --args '{"module":"sd"}'
```

结果分开看：自动检查成功不等于物理效果确认。真实联网、蓝牙目标配对、可听清的音量等必须在对应环境完成。完整恢复另有备份和写入范围核验，不在普通测试按钮中擦写固件。
