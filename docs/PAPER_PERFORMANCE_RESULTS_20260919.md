# PAPER 流畅度与无线设置：执行记录

日期：2026-09-19。主工程 `outputs/metalio-sdk-1.0`，沿用现有脏工作区，未提交或推送 Git；未覆盖用户已有改动。

## 实施范围

- 字体：分离界面/正文索引预算、缓存已验证字体身份、哈希字形缓存；同一个标签只获取一次字形；保留介质代际失效、CRC、SHA256及缺字错误检查。
- 冷加载：完整字库索引改为每批128条读取，保留逐条语义校验；生产界面子集更新为611字符、75规格，原完整字库保留。
- 阅读：同一本书重开复用解析元数据；双章节及有界排版缓存；打开书籍期间禁止替换书籍文件；翻页保存记录仍同步完成。
- FAT32：修正双槽记录在已有文件上发布失败的问题。只清理待更新的非活动槽，保留另一份CRC有效记录；校验槽位奇偶与代数。
- 刷新：普通黑白页面复用快速刷新；初次、显式全刷、灰阶切换和积累15次局刷后保留全刷。硬件等待时释放GUI锁，屏幕所有权、延迟重绘、全刷请求和关机路径协同。
- 无线：复用官方STA驱动，加入受控扫描/连接工作任务、中文无线页面、密码隐藏输入、结果和超时；连接成功才保存配置。扫描器、定位和电源恢复共用互斥；自检不再另外初始化Wi-Fi栈。
- 调试：保留统一USB维护入口。协议代际改变时重新握手但保持串口，避免启动确认前因关闭串口触发额外USB复位。`paper.network`提供只读状态。

## 主机检查

- 24项Python测试通过（service、acceptance、hardware_acceptance、paper_package）。
- 原生性能测试通过：索引预算分离、LRU命中、校验缓存、介质失效、坏字体拒绝、书籍租约、缓存失效、灰阶能力拒绝、记录多轮轮换及坏记录/失败发布回退。
- 原生6页生成和生产资源验证通过；5个原有页面与前版像素一致。
- UI token生成一致性、`git diff --check`通过。
- ESP-IDF 5.5.4、Metalio专用配置构建通过，perf2应用2688864字节，5MiB应用槽剩余49%。该比例是单个应用槽，不能等同整片Flash空闲。

## perf1 实测与保留的失败证据

设备身份 `1020ba6e0be0`。原始日志在 `artifacts/paper-perf1-20260919/`。

- 旧版界面切换约2.6–2.9秒，冷打开现有EPUB为32.105秒；旧版下一页28.952秒后报“提交文件失败”。这次失败不能作为成功翻页速度的基线。
- perf1暖页面约1.0–1.5秒，下一页/上一页约1.3–2.8秒，返回继续阅读约1.3–1.5秒；冷开书仍约26秒、首次无线页约20秒，因此继续实施perf2。
- 无线扫描在实机完成，获得15个接入点；未提供密码，未将联网标记为PASS。
- 初次perf1写入和设备内原始分区哈希读回成功，启动时串口重开引发额外USB复位，设备回退到maint3。保留 `install.jsonl` 和原服务日志。修正服务后再次安装，同镜像启动perf1通过，记录 `install-rehandshake-fixed.jsonl`。
- 上述启动回退属于实际观察，完整恢复演练、故意断电注入仍为NOT_PROVEN。

## perf2 安装清单

- 固件：`artifacts/paper-perf2-20260919/update-package/update.bin`
- SHA256：`7255e9b6d496bcc89398401c52d9d1641097f54b3987a37340c6a750d363b495`
- 文件2688864字节；应用写入`0x80000–0x31075F`，扇区范围截至`0x310FFF`；启动选择区`0xD000–0xEFFF`。
- 当前perf1保留在`0x580000`，旧maint3主机读回文件和完整原厂备份保留。未写启动程序、分区表、模型、旧字体分区、eFuse。
- SD旧升级包保存在`paper/updates/previous-perf1-before-perf2/`；旧界面小字库保存在`paper/fonts/ui-backup-before-perf2/`。完整中文字库和用户电子书不变。
- 安装完成：设备内备用分区写入和原始分区SHA读回通过；运行/启动地址均为`0x80000`，`app_version=1.0.0-paper-perf2`，boot ID `12a4ad45dbfce400`，启动已接受。证据：`artifacts/paper-perf2-20260919/install.jsonl`。本轮没有另做ROM串口外部全分区读回。

### perf2 真机自动结果

`device-performance.jsonl`包含29次完成的页面操作，按revision=presented且last_action匹配验收，错误字段为空。中位值：

| 操作 | 秒 | 结论 |
| --- | ---: | --- |
| 设置 | 1.617 | 暖切换改善 |
| 书库 / 维护 / 传输 | 1.531 / 1.531 / 1.530 | 暖切换改善 |
| 下一页 / 上一页 | 1.532 / 1.532 | 成功翻页和位置记录 |
| 继续阅读 | 1.532 | 同一会话重开缓存命中 |
| 首次无线页面 | 2.062 | 子集字库补齐后改善 |
| 无线扫描触发页面 | 1.798 | 另以busy=false、扫描完成确认扫描结束 |
| 扫描结果页面 | 9.412 | **仍慢**，含网络名称字形首次加载 |
| 冷打开EPUB | 27.614 | **未解决，不能标记整体流畅度完成** |

扫描读到10个接入点（上轮15个，扫描环境变化可导致数量不同）。尚未输入密码取得IP，联网不是PASS。

`maintenance-acceptance.jsonl`：页面和字体错误为空、独立诊断文件写入/读回SHA一致、30次心跳boot ID稳定。设备上未删除用户文件。

`runtime-smoke.jsonl`：关闭无线后20轮状态/心跳查询、20次往返翻页、3轮PAPER刷新期间请求自检页面再恢复PAPER、电子书SHA复核通过，boot ID未变。最后回到首页，输入队列0、触摸读取错误0、协议丢包0，内部堆52247字节、PSRAM5954244字节、USB任务栈低水位8404字节。该测试是有界冒烟，不能证明所有竞争条件或8小时稳定性；缓存加载后的内存下降也不能据此直接判定泄漏。

另外实测 `paper.file hash`：26px/400正文字库4.149秒，EPUB 1.125秒，电子书SHA与已知原文件一致。**这只量出了两项校验，剩余冷加载时间不能全部归因于字体或解析。** 下一步需要在font hash/PGF校验/索引载入、EPUB解析和首屏字形读取各加独立计时，再评估一次流式校验与批量字形读取。禁止为提速跳过完整性验证。

本轮普通黑白页面使用快速刷新，积累15次局刷后仍会全刷；这保留了墨水屏清残影策略。主代理未通过肉眼确认残影和触摸手感，故物理观感为NOT_PROVEN。

## 独立审查

子代理逐轮只读审查发现并推动修复：刷新所有权、全刷请求丢失、书籍缓存失效、FAT槽位校验、重复Wi-Fi初始化、关机锁竞争，以及重新握手异常分支关闭串口的问题。

最新静态结论无新增安全P1；`Session.connect`的DeviceRebooted异常分支已补齐，增加hello和ready阶段两个用例。子代理独立运行24项主机测试通过，并核对安装和29次完成操作的日志，明确仅可阶段交付：冷开书和扫描结果页仍慢。子代理未操作实机；静态/日志审查与主代理实机测试是两类证据。

## 已知边界与后续

本轮交付覆盖流畅度核心、无线设置和USB维护一致性。计划中的蓝牙配对/音频服务、录音笔、时间/电源完整设置、其他传感器公共服务尚未全部接入。没有声称SDK 1.0整体完成。

仍需人工：刷新残影和触摸手感；在中文无线页选网络并输入密码。未读取Mac保存的Wi-Fi密码。没有执行完整原厂恢复或破坏性断电测试。
