# finish1 收尾验收

范围：JPEG黑块、冷开、续读、联网、稳定性、原厂恢复演练、GitHub保存。当前工作以clear1为基线，不引入真灰阶。

## 实现与证据（持续补齐）

- JPEG：私有TJpgDec在JD_FORMAT=2时缺失MCU输出分支；补充Y块栅格排列和0/1/2/3缩放。问题图及三种色度采样16组Pillow独立参考对比PASS，最大平均绝对误差1.018/255。后续扩大图片集合；旧73张“返回成功”不能作为像素验收。
- 字体：既有PFV2只覆盖18/26px常规和32px中等；34px首次全读约7.3MiB。本轮扩展所有现存16–40px常规字号；校验索引上限256KiB，仍受总索引预算与固件固定SHA约束。字库原字节不变，不能把未安装证明文件时的fallback路径标为提速。
- 继续阅读：设置/首页返回时复用保持打开的Reader，避免关闭重建及重新定位。USB交接/改名/排版引擎切换仍先关闭。章节边界跨重启另验收，不用同进程返回替代。
- 网络：增加底层断开reason，中文区分热点缺失、认证和DHCP；没有证据时不把密码错误写成结论。
- USB健康：保留累计dropped，新增输入溢出、发送超时、超长包、编码失败、任务创建失败计数；状态包去掉可能无限增大的字体规格列表，仅保留family_count。老版2037次累计丢包无法追溯归类，不改历史FAIL。

## 恢复边界

用户确认现在可配合，要求需要动作时弹窗提醒。先读取当前16MiB、校验，再恢复原厂16MiB并验证原厂界面，最后恢复当前开发快照。操作eFuse禁止；保护启用即停止；所有文件/地址/影响范围在写前报告。备份与运行证据仅本地，不上传GitHub。

2026-09-20：安全查询Disabled/Disabled，设备ID匹配；当前16MiB备份SHA256 `153b5f82ef27078a25eabde7c13b47fae6c4ae590ed155f322ec312b3faaa2ad`，esptool整片比对通过。原厂16MiB写入/额外verify-flash摘要一致；重启时端口重枚举导致工具退出1，单列为启动交接异常。用户随后明确确认“已显示原厂界面”，原厂恢复物理PASS。

开发快照16MiB写回及独立verify-flash均PASS。重新连接确认clear1、0x80000、boot `cd2178f30457f040`，Reader恢复34px常规字重和网点模式，无应用/字体错误。完整原厂→开发快照往返PASS；此证据不等同于OTA自动回滚测试。日志和整片镜像保存在本地`artifacts/paper-finish1-recovery-20260920`，不进入公开仓库。恢复后无人读取USB期间累计dropped=50，保留记录；不能将这次启动用作零丢包长测。

## 电脑端测试

- Python测试218 PASS。
- JPEG扩大到188组，所有四种缩放与独立Pillow参考对比PASS，最大MAE 1.461/255。
- ASan/UBSan：清晰度/续读、CrossMux、16组JPEG、所有25个常规字号的字体证明及损坏处理PASS。
- finish1构建PASS，镜像3042656字节；单5MiB应用槽剩2200224字节。SHA256 `7627274c81b103f6a8073429f00fc2306aaccfde2c3389549903fd6d2cdabdfd`。

## 安装

SD 26份PFV2逐一核对实际字库头部、索引摘要和总大小，拷贝后验证辅助文件SHA；原PGF不变。旧clear1更新包归档到SD的`paper/updates/previous-clear1-before-finish1`。macOS首次安全弹出被Spotlight占用拒绝，等待后正常弹出成功，无强制卸载或用户数据删除。

finish1写入`[0x580000,0x866D60)`，扇区范围至`0x867000`，clear1保留0x80000。SD包SHA、应用原始读回、新启动PASS，boot `f15e7e762517a2ba`，首次握手dropped=0，各分类losses=0。`install.jsonl`为本地证据。未修改eFuse或分区表。

## 待完成

实机安装与原厂/开发系统恢复往返已完成。后续缓存稳定化、同定位速度对比和联网结果见`STABILIZATION_20260920.md`；JPEG物理页、续读重启、连续稳定性与公开提交依各项最新证据判断。长测必须实际达到时长和最终PASS，模拟时钟单测不计入设备稳定性。
