# 2026-09-19：PAPER 中文维护与字体修复

## 本轮交付

- 版本：`1.0.0-paper-maint3`，ESP-IDF 5.5.4，Metalio E-Ink4 专用配置。
- 设备：`1020ba6e0be0`；实读安全启动、Flash 加密均未开启；未改 eFuse。
- 入口：首页 → 系统设置 → 开发与维护。
- PAPER / GLYPH 3 样式参数与公共绘制组件继续作为界面规范。
- 维护操作、提示、应急页和待机提示使用中文；保留 USB、版本、路径等技术标识。
- 缺 SD 字体或 USB 磁盘占卡时，Flash 内的小型 MiSans 中文子集维持维护页面可用。

## 写入与恢复证据

最终应用 2,785,344 字节，SHA256：
`162977eaec4880b958fdf97502598b10743fb250cbcf213ffb056b81f898c876`。

通过设备内更新服务从 SD 安装：校验机型、芯片、版本、大小、SHA256，写备用分区，
完整读回后切换启动选择。本轮先验证 `ota_0 → ota_1`，再安装修正版 `ota_1 → ota_0`。
最终运行地址 `0x80000`，镜像结束 `0x32803f`，扇区擦除结束 `0x328fff`。
启动选择区 `0xd000–0xefff` 由更新服务修改；启动程序、分区表、原字体分区未写。
另经 ROM 工具独立读回最终应用，逐字节比较和 SHA256 一致。

证据目录：`artifacts/paper-maint1-20260919/`（含本轮三个迭代，目录名保留初始编号）。

- 最终包：`update-package-maint3-final/update.bin` 与 `update.json`。
- 最终读回：`app0-maint3-readback.bin`。
- 上一中文版本：`app1-maint2-readback.bin`，SHA256 `c0c7ad1a070b4081ce8110815709db2ca7f7a89f972877f155d894cb5b9edc8f`。
- 初始 ota_0 全槽备份：`app0-before.bin`，5 MiB，SHA256 `b635a670eb166a657b07a8eaba0b11018667159ba645b4fbb42d5d94c2a3a1f0`。
- ota_1 写前全槽备份：`app1-before.bin`，5 MiB，SHA256 `16f34632ed5fc8556743bb4a5b403edd2cf9845372951b48914b61597a4a0ba7`。
- 写前启动区：`boot-partitions-ota-before.bin`，64 KiB。
- 原厂 16 MiB 备份继续保留，SHA256 `5b7986151b18163f9087611edfedd563c49e37c32c12d999d0e0fa3d82a1cfaf` 本轮重新核验。

独立原厂备份仍在相邻 `metalio-eink4-backup-2026-09-15` 目录。
回退时要核对实际启动槽：刷一个应用地址并不自动改变启动选择。
优先使用维护更新服务；ROM 恢复按实读分区与备份实施，禁止盲用默认 `flash_args`。

## 空间与资源

- 上版 PAPER 应用：4,278,800 字节；本版减少 1,493,456 字节，约 1.42 MiB。
- 5 MiB 应用槽剩余 2,457,536 字节，约 2.34 MiB / 47%。这是应用槽余量，不是整片 Flash 空闲量。
- 5 MiB 旧字体分区和另一个 5 MiB 应用槽继续保留，没有虚报释放。
- SD 完整阅读字库：75 份、443,324,038 字节；界面小字库：75 份、8,341,066 字节。
- MiSans 16–40 px、400/500/700 字重现有选项继续保留。字号梯级收拢仍为建议，未替用户切换默认策略。
- 书籍保留在 `/books/`；系统字体在 `/paper/fonts/`，拼音词库在 `/paper/ime/`。

第一次资源安装在 75 份完整字库逐文件核验、发布清单后，因中文增补导致小字库清单更新而拒绝继续。
发现的 `.provisioning` 内容与新源 SHA 一致。完整阅读字库未重传；新 UI/IME 共 80 文件另行校验安装，
回执 `sd-ui-ime-install.json`。完整字库的 SD 清单与源清单精确一致，75 份文件大小复核一致。
没有删除用户书籍，没有格式化卡。

## 已通过与限制

- PASS：主机 pytest 59 项；另有 139 项旧 FontLab 素材依赖测试跳过，未算通过。
- PASS：原生 C++ 实际资源渲染 5 页面、150 份字体、UI 缺字转同字体完整字库、USB 资源锁与重接、拼音候选。
- PASS：设计参数文件与生成的 C++ 常量一致；`git diff --check` 通过。
- PASS：最终固件 ROM 独立读回；设备版本/身份、运行槽和中文维护状态正确。
- PASS：设备常规资源检查：75 份 UI 字库内容、75 份阅读字库头/规格/长度、实际拼音候选查询。
- PASS：两次电脑安全弹出后 USB 调试重新出现，重连后 SD 已挂载、首页 `font_error` 清空。
  重连观察到新 `boot_id`，不能把它称为无重启、同一会话。USB 磁盘与 USJ 调试仍分时占用接口。
- PASS：最终首页、设置、维护、书库、传输页面导航，字体错误为空；SD 小文件传输、设备端 SHA256 读回一致；30 次连续心跳维持同一 `boot_id`、丢包为 0。日志 `device-acceptance-final.jsonl` 与 `service-final.jsonl`。
- PASS：USB 任务栈从 8 KiB 增至 16 KiB；相同文件校验/传输路径后最低余量 8,556 B（旧版约 360 B）。最终查询普通堆 75,051 B、PSRAM 7,645,344 B；这些是本次快照，不是长期稳定性结论。
- 已解释的早期失败：全量字库深检占用界面工作线程，导致并行页面验收的返回首页排队超时。
  原失败日志保留；本版把常规与深检分开，增加进度/时间预算，最终验收顺序运行。
- NOT_PROVEN：设备端完整阅读字库深检。旧版无进度扫描超过十分钟，被安全中止；没有把它记录为通过。
  完整字库在电脑安装时已逐文件读回核验，实际读取时仍执行内容校验。
- NOT_PROVEN：本版最终物理清晰度/触摸手感、故意启动失败的自动回滚、整片原厂恢复演练。
  双槽更新成功与保留恢复材料不等于上述演练已经完成。

本轮不声称无线网络、蓝牙、录音应用或整个 SDK 所有硬件能力已经完成验收。
